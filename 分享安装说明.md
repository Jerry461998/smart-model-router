# Smart Model Router 分享安装说明

这是一个面向 Windows + Codex / ChatGPT Desktop 的 Smart Model Router 分享包。

## 先看清楚它会改什么

运行 `INSTALL.ps1` 后，安装器会：

1. 把插件复制到 `%USERPROFILE%\plugins\smart-model-router`。
2. 安装 6 个 Codex 自定义 worker 配置到 `%USERPROFILE%\.codex\agents`。
3. 修改 `%USERPROFILE%\.codex\config.toml`，把默认根模型设为 `gpt-6-sol`、reasoning effort 设为 `medium`，并开启 agents。
4. 在 `%USERPROFILE%\.codex\AGENTS.md` 中加入 Smart Model Router 的托管规则块。
5. 更新个人 marketplace：`%USERPROFILE%\.agents\plugins\marketplace.json`。
6. 调用 `codex plugin add smart-model-router@personal` 完成插件注册。

安装器会在修改配置前创建备份；卸载器只移除它自己管理的内容，并尽量恢复安装前设置。

## 对方本机安装

前提：

- Windows
- 已安装并登录 Codex / ChatGPT Desktop
- `codex` 命令能在 PowerShell 里运行
- 对方账号能使用插件里配置的模型；模型可用性受账号 / Workspace 策略影响
- 当前配置使用 GPT-6 Luna、Sol 和 Astra；GPT-6 家族没有 Terra 型号。

步骤：

1. 解压整个压缩包，不要只单独拿插件子文件夹。
2. 打开 PowerShell，进入解压后的目录。
3. 执行：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\INSTALL.ps1
```

4. 安装完成后，**新建一个 Codex 任务 / 会话**，让插件和 Skill 重新加载。
5. 可执行：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\STATUS.ps1
```

检查安装状态。

## 卸载

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\UNINSTALL.ps1
```

## 通过 GitHub 给 Workspace 分发

这个分享包已经带有：

```text
.agents/plugins/marketplace.json
plugins/smart-model-router/.codex-plugin/plugin.json
```

所以也可以直接把**解压后的整个目录内容**推到一个 GitHub 仓库。

Workspace 管理员随后可在 ChatGPT：

`Workspace settings / Admin -> Plugins -> Add -> Import marketplace`

填写该 GitHub 仓库地址进行导入。Marketplace 已使用仓库内本地路径 `./plugins/smart-model-router` 指向插件。

注意：GitHub Marketplace 导入属于 Workspace 管理功能；个人账号之间不能保证仅靠一个分享链接直接安装。

## 安全检查

此分享版已移除：

- `__pycache__`
- `.pyc`
- 原项目开发过程中的 `findings.md / progress.md / task_plan.md`

并扫描了常见 API Key、Token、私钥、邮箱和绝对用户路径；未发现需要随包删除的真实密钥。
