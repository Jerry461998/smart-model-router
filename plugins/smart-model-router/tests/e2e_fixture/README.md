# Synthetic announcement fixture

This isolated fixture exists only for the Smart Model Router acceptance simulation. It is intentionally initialized with a per-user rather than per-user/per-announcement confirmation bug so real routed workers can explore, implement, and verify the requested behavior without touching any user's project code.
